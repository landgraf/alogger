with "directories";
project alogger is 
    for Source_Dirs use ( Directories.Libdir & "/alogger/**"); 
    for Object_Dir use Directories_Libdir & "/alogger/";
    for Library_Dir use Directories_Libdir & "/alogger/";
    for Languages use ("Ada");
    for Library_Kind use "relocatable";
    for Library_Name use "alogger";
    for Externally_Built use "True";
end alogger; 
